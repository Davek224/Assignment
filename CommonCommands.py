import conftest

def __init__(self, driver):
        self.driver = driver

    def welcome_link_is_displayed(self, Element):
        try:
            self.driver.Element.isDisplayeD()
            print(element)
            if Element == True:
                pass
        except Exception as e:
            print(e)