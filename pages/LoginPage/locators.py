from selenium.webdriver.common.by import By

class Page:
        username_id = By.ID, "username"
        instance_id = By.ID, "instance"
        password_id = By.ID, "password"
        login_button_id = By.ID, "login-submit"